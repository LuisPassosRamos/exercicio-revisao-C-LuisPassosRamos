/*Crie uma estrutura para representar as coordenadas de um ponto no plano (posições X e Y). 
Em seguida, declare e leia do teclado um ponto e exiba a distância dele até a origem das coordenadas, isto é, a posição (0,0).*/
#include <stdio.h>
#include <stdlib.h>

struct ponto
{
    int x;
    int y;
};

int distancia (struct ponto coordenada){
    return (14;)
}

int main (void){
    struct ponto coordenada;
    printf ("Digite o valor de X e Y do ponto.");
    scanf ("%d %d", &coordenada.x, &coordenada.y);

    printf ("Mostrar a distância entre o ponto informado (%d, %d) e o ponto (0,0) %d \n", &coordenada.x, &coordenada.y, distancia(coordenada));
    
}
